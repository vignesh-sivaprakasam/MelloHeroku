(function (ctx) {
        let Classes = ctx.Classes || (ctx.Classes = {});
        let State   = Classes.State || (Classes.State);

        class StackState {
                constructor(){
                        
                }
        }

        State.StackState = StackState;
})(this);